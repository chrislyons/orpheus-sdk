---
source: standard-notes
sn_filename: "ORP019-9ded44a0.txt"
prefix: orp
original_format: lexical
imported: 2026-05-01
status: archive
related:
  - cpp20_for_audio_dsp
  - cmake-googletest-ci
  - audio_processing_requires_determinism
---

Here is a project status report generated by Codex Cloud after reviewing our orpheus-sdk repo:



{



Orpheus SDK Status Report

Summary

The project has completed its foundational milestone by quarantining legacy code, standing up a modernized core, shipping reference adapters, and enabling CI and sanitizer coverage, while feature-expansion milestones remain ahead.



The repo targets Orpheus SDK v0.2.0, providing a compact C++20 core with reference adapters for REAPER and a standalone minhost utility, built for cross-platform workflows.



Core Library

Public headers expose the ABI negotiation API, transport state structures, session/track/clip handles, and render descriptors alongside C++ helpers for version negotiation.



SessionGraph manages tracks and clips, enforces minimum clip lengths, tracks tempo/transport metadata, and supports clip sorting and session range management for host synchronization.



JSON helpers provide a bespoke parser/serializer, filesystem load/save helpers, and deterministic render filename generation to support session interchange and tooling.



The ABI implementation guards calls, maps handles back to SessionGraph, normalizes click render specs, synthesizes WAV data, and exposes negotiation fallbacks, while full multitrack rendering is still stubbed out.



Adapters

The CLI minhost parses session/render options, loads JSON sessions, instantiates session/clip grid resources via the ABI, renders click tracks, or simulates transport when offline render paths are omitted.



The REAPER adapter maintains a guarded panel snapshot, imports sessions through the ABI tables, drives click rendering requests, and exposes extension entry points and panel text summarizing session state.



Tooling & Tests

Smoke tests cover ABI negotiation, session creation, tempo mutation, and clip-grid operations to ensure the core tables behave as expected.



Conformance tooling round-trips bundled fixtures and validates deterministic click filenames, reinforcing schema stability alongside shared GoogleTest infrastructure.



Repository guidance highlights sanitizer integration, static analysis enforcement, and CI coverage across supported platforms.



Build & Packaging

CMake options toggle adapters, tests, LTO, and shared builds; sanitizer flags and compiler-specific settings are wired in, and install/export rules produce SDK packages with CPack ZIP artifacts.



The core library target enables C++20 features, propagates sanitizer link flags, and centralizes warning configuration for downstream components.



Repository Hygiene

Historical SDK content is isolated under backup/non_orpheus_20250926, preserving legacy assets without polluting active development, aligning with the recorded repository audit.



Outstanding Work / Risks

Multitrack rendering through render_tracks is unimplemented, limiting audio export scenarios beyond the click track path.



Roadmap milestone M2 calls for richer session models, deeper REAPER integration, expanded minhost rendering, and integration tests, which remain future objectives; production-hardening items in M3 are also pending.



}



Using the project files available to you, advise on a plan to move this project forward. Deliver a set of prompts optimized for Codex Cloud to move through tasks in a logical progression.
